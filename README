To build:

$ rpmdev-setuptree
$ git clone git@git.corp.yahoo.com:qbarnes/hello-rpm.git
$ cd hello-rpm/SPECS
$ spectool -g -R hello.spec
$ rpmbuild -bb hello.spec
$ ls -l $(rpm -E '%{_topdir}')/RPMS/*/hello*.rpm
